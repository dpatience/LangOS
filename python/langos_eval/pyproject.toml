[project]
name = "langos-eval"
version = "0.1.0"
description = "LangOS evaluation harness"
requires-python = ">=3.11"
dependencies = ["langos-sdk @ file:///./../langos_sdk"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
